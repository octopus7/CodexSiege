# Paste-ready MCP prompt — session setup

Use `get_scene_info` first. Do not delete or overwrite an existing user scene. If the current
file is not an empty dedicated Ironwall Siege file, stop and ask me to open a new Blender file.

For a clean file:

1. Save it as `IronwallSiege_AssetKit.blend`.
2. Set Metric units with Unit Scale 1.0.
3. Create collections `FORTRESS`, `SIEGE_ENGINES`, `UNITS`, `GROUND`, `COLLISION`, and `EXPORT`.
4. Create materials named `M_Stone`, `M_Wood`, `M_Iron`, `M_Cloth`, and `M_Leather`.
5. Use late-medieval European construction, realistic proportions, modular game-ready topology,
   and no fantasy ornament.
6. Do not use Sketchfab, Poly Haven, Hyper3D, Hunyuan3D, network downloads, or external scripts.
7. Save, then return a concise scene summary. Do not start asset modeling yet.

Coordinate contract: meters in Blender, Z up, each asset faces local +X, pivot at ground center,
apply rotation and scale before FBX export.
