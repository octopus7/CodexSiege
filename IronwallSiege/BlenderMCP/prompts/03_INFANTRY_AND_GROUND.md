# Paste-ready MCP prompt — infantry and ground

Read the asset manifest and model only `SM_Unit_Infantry_A` and `SM_Ground_SiegeField`.

The infantry asset is a static, neutral-ready late-medieval foot soldier at 1.85 m overall height:
simple helmet, padded or mail torso, boots, heater shield, and spear. Keep shield and spear as
separate children. Avoid identifiable heraldry and avoid a dynamic pose so the asset can be rigged
later.

The ground is a low-density 90 m × 84 m tiling battlefield patch. Use subtle macro undulation but
keep the boundary vertices level enough to tile. Geometry must not carry tiny rocks or grass;
those belong to later scatter systems.

Apply the coordinate, pivot, UV, collision, LOD, naming, and screenshot checks in the manifest.
Save after each asset.
